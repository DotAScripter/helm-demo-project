apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "jpod.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ template "jpod.name" . }}
      release: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: {{ template "jpod.name" . }}
        release: {{ .Release.Name }}
    spec:
      containers:
        - name: jpod
          image: {{ .Values.image.repository }}:{{ .Values.image.tag }}
          ports:
            - containerPort: {{ .Values.env.jpod.port }}
          env:
            - name: JPOD_SERVICE_IP
              value: {{ quote .Values.env.jpod.host }}
            - name: JPOD_SERVICE_PORT
              value: {{ quote .Values.env.jpod.port }}
            - name: JPOD_HTTP_PORT
              value: {{ quote .Values.env.jpod.httpport }}
            - name: CPPOD_SERVICE_IP
              value: {{ quote .Values.env.cppService.host }}
            - name: CPPOD_SERVICE_PORT
              value: {{ quote .Values.env.cppService.port }}
            - name: PYPOD_SERVICE_IP
              value: {{ quote .Values.env.pyService.host }}
            - name: PYPOD_SERVICE_PORT
              value: {{ quote .Values.env.pyService.port }}
